# ============================================================
# VARIÁVEIS DE AMBIENTE — ICE VAN ADMIN
# Edite este arquivo com suas credenciais reais antes de rodar
# ============================================================

# Conexão MySQL — mesmo valor do .env (para o Next.js carregar)
# Formato: mysql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO
DATABASE_URL="mysql://root:icevans123@localhost:3307/icevans"

# Credenciais do painel administrativo (acesse /admin)
# Troque por valores reais antes de ir para produção!
ADMIN_EMAIL="admin@icevans.com.br"
ADMIN_PASSWORD="icevans@admin2025"

# Chave secreta para assinar os tokens JWT
# Gere uma string aleatória com: openssl rand -base64 32
JWT_SECRET="icevans-jwt-secret-mude-em-producao-32chars"
