# ============================================================
# VARIÁVEIS DE AMBIENTE — ICE VAN SITE
# ============================================================
# Copie este arquivo para .env.production no servidor
# e preencha com os valores reais de produção
# ============================================================

# ──────────────────────────────────────────────────────────
# APLICAÇÃO
# ──────────────────────────────────────────────────────────

# Nome da aplicação
APP_NAME="Ice Van Site"

# Ambiente (production, development)
APP_ENV=production

# Debug (false em produção)
APP_DEBUG=false

# URL pública do site (usado para SEO e links absolutos)
APP_URL=https://seusite.com.br
NEXT_PUBLIC_SITE_URL=https://seusite.com.br

# ──────────────────────────────────────────────────────────
# BANCO DE DADOS MYSQL (Hostinger)
# ──────────────────────────────────────────────────────────

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=u123456789_icevans
DB_USERNAME=u123456789_icevans_user
DB_PASSWORD=sua_senha_mysql_aqui
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# URL de conexão Prisma (gerada automaticamente a partir das variáveis acima)
# Formato: mysql://USUARIO:SENHA@HOST:PORTA/BANCO
DATABASE_URL="mysql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}?schema=public"

# ──────────────────────────────────────────────────────────
# AUTENTICAÇÃO ADMIN
# ──────────────────────────────────────────────────────────

# Email do administrador (login no painel /admin)
ADMIN_EMAIL="admin@icevans.com.br"

# Senha do administrador (login no painel /admin)
ADMIN_PASSWORD="sua-senha-segura-aqui"

# Chave secreta para assinar tokens JWT (gere com: openssl rand -base64 32)
# IMPORTANTE: Altere em produção!
JWT_SECRET="sua_chave_secreta_muito_longa_e_segura_aqui"

# Tempo de expiração do token JWT em segundos (86400 = 24 horas)
JWT_EXPIRY=86400

# ──────────────────────────────────────────────────────────
# EMAIL SMTP (Hostinger)
# ──────────────────────────────────────────────────────────

MAIL_HOST=smtp.hostinger.com
MAIL_PORT=587
MAIL_USERNAME=noreply@seudominio.com.br
MAIL_PASSWORD=sua_senha_email_aqui
MAIL_FROM_ADDRESS=noreply@seudominio.com.br
MAIL_FROM_NAME="Ice Van"

# Variáveis alternativas para compatibilidade
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=noreply@seudominio.com.br
SMTP_PASS=sua_senha_email_aqui
SMTP_FROM=noreply@seudominio.com.br

# ──────────────────────────────────────────────────────────
# RATE LIMITING (Opcional)
# ──────────────────────────────────────────────────────────

# Limite de requisições para rotas públicas
RATE_LIMIT_PUBLIC=100

# Limite de requisições para rotas autenticadas
RATE_LIMIT_AUTH=1000

# ──────────────────────────────────────────────────────────
# CORS (Origens permitidas separadas por vírgula)
# ──────────────────────────────────────────────────────────

CORS_ORIGINS=https://seusite.com.br,https://www.seusite.com.br
